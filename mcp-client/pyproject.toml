[project]
name = "mcp-client"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Mohammed-web1", email = "nokrymohammed342@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.122.0",
    "mcp>=2.0.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
mcp-client = "mcp_client:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
