[package]
name = "mcp-server-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
# Dependencies for MCP server, async runtime, and email parsing will go here.
tokio = { version = "1", features = ["full"] }
