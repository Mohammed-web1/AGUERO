# Agent Rules and Workflows (private to this dev environment)
.agents/

# Environment Variables — secrets, never commit
.env
.env.local
# NOTE: .env.example is deliberately NOT ignored -- it carries no secrets and is
# the file the README tells you to copy to .env.

# Rust / Cargo
**/target/
**/*.rs.bk
Cargo.lock.bak

# Python
__pycache__/
*.py[cod]
*$py.class
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
.pytest_cache/

# Python Virtual Environments
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# OS Generated Files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDEs and Editors
.vscode/
.idea/
*.swp
*.swo

# Docker
docker-compose.override.yml
# NOTE: .dockerignore is deliberately NOT ignored -- without it a fresh clone
# builds images with .env and .venv/ in the build context.

# Dev-only helper scripts (not part of the deployable product)
test_mcp.py
register_user.py

